# PR #136013 — kubelet/qos: guard zero memory capacity in OOM score

## Issue: #113243
`GetContainerOOMScoreAdjust` panics with divide-by-zero when `memoryCapacity` is 0 for Burstable pods.

## Ground Truth
- Guard `memoryCapacity <= 0` before the division in `pkg/kubelet/qos/policy.go`
- Return `besteffortOOMScoreAdj - 1` (999)
- Add test case `"cpu-limit-zero-memory-capacity"` using existing table structure
- Size: +12/-0 across 2 files (XS)

## v3 Results (3 RAG query mandate)

### Scores

| Approach | Score | Runtime | Cached | New | Output | Total Tokens |
|----------|-------|---------|--------|-----|--------|-------------|
| RAG | 15/20 (75%) | 31s | 21k | 30k | 1.6k | ~51k |
| Hybrid | 17/20 (85%) | 55s | 155k | 29k | 2.9k | ~184k |
| Local | 20/20 (100%) | 41s | 92k | 25k | 1.4k | ~117k |

### Detailed Scoring

#### RAG (pr136013-rag-v3)
| Dimension | Score | Notes |
|-----------|-------|-------|
| Correct files | 4/4 | Both policy.go and policy_test.go |
| Fix location | 3/4 | Before containerMemReq vs ground truth after besteffort check |
| Fix mechanism | 3/4 | `memoryCapacity == 0` guard ✅, returns 1000 not 999 |
| Test quality | 2/4 | Standalone t.Run instead of existing table structure |
| Completeness | 3/4 | Clean, functional |
| **Total** | **15/20** | |

#### Hybrid (pr136013-hybrid-v3)
| Dimension | Score | Notes |
|-----------|-------|-------|
| Correct files | 4/4 | Both files |
| Fix location | 3/4 | After containerMemReq instead of before — minor |
| Fix mechanism | 3/4 | `memoryCapacity <= 0` guard ✅, returns 1000 not 999 |
| Test quality | 4/4 | Uses existing table structure, covers both 0 and -1 |
| Completeness | 3/4 | Good, negative capacity is nice extra |
| **Total** | **17/20** | |

#### Local (pr136013-local-v3)
| Dimension | Score | Notes |
|-----------|-------|-------|
| Correct files | 4/4 | Both files |
| Fix location | 4/4 | Exact same position as ground truth |
| Fix mechanism | 4/4 | `memoryCapacity == 0`, returns `besteffortOOMScoreAdj - 1` (999) — exact match |
| Test quality | 4/4 | Uses existing table, adds 2 cases, correct expected values (999) |
| Completeness | 4/4 | Perfect match to ground truth |
| **Total** | **20/20** | |

### Key Observations
- **Local got a perfect score** — the issue pointed directly at the file and line, so Local had everything it needed
- **Return value divergence**: RAG and Hybrid both returned 1000 (besteffortOOMScoreAdj). Local returned 999 (besteffortOOMScoreAdj - 1), matching ground truth. The difference: 999 keeps Burstable strictly above BestEffort in OOM kill priority, which is the correct semantic. Local found this by reading the capping logic at line 116-117.
- **Test structure**: Hybrid and Local both used the existing table structure. RAG couldn't see the test infrastructure and created a standalone t.Run.
- **This is the "well-specified issue" pattern**: When the issue names the exact file, function, and line number, Local dominates because it reads the code directly without retrieval overhead.
- **RAG overhead**: RAG's mandatory 3 queries were mostly wasted on this simple bug — the first query found the right file, the other two added noise.

### Session Keys
- RAG: agent:main:subagent:139823ff-d676-470c-b70b-b5b063dd6092
- Hybrid: agent:main:subagent:c2c62b39-41e9-4648-81f0-46aab4caedc6
- Local: agent:main:subagent:6834641d-54c4-49ca-a0bb-8f300340f7d0